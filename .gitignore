# ---- raw BehaviorSpace exports -----------------------------------------------
# The exports are distributed gzip-compressed (data/validation/*.csv.gz); the
# uncompressed copies are produced locally by run_all.sh and are not tracked.
data/validation/*.csv

# ---- intermediate splits, not needed to reproduce anything ------------------
data/wide/

# ---- superseded / working copies of the data workbook -----------------------
data/empirical/Japan 1994~2018 data collection.xlsx
*.xlsx.bak
~$*

# ---- run artefacts and OS cruft ---------------------------------------------
logs/
archive/
.DS_Store
__pycache__/
*.pyc
